# moonbit-spice：可扩展电路仿真内核

moonbit-SPICE 面向模拟电子、电源设计和教学实验，提供纯 MoonBit 的 SPICE-like 网表解析、改进节点分析和电路仿真基础库。它与 MoonElec 的公式计算定位不同，核心链路是“输入电路模型，经过求解后输出节点电压、支路电流和可诊断结果”，让 MoonBit 生态拥有一块可复用的仿真基础设施。

首期交付六类器件：电阻、电容、电感、二极管、独立电压源和独立电流源；完成工程后缀解析、错误节点定位、MNA 矩阵组装、带选主元的线性求解、DC 工作点、CSV/文本结果输出和收敛/奇异矩阵诊断。API 以 `Device`、`Circuit`、`MatrixSolver` 演进接口和 `SimulationResult` 为核心，保持器件模型、分析器、求解器和输出层解耦，便于后续加入 AC 复数小信号、瞬态积分、Newton 非线性迭代、BJT/MOSFET、参数扫描、蒙特卡洛和 WASM 波形展示。

项目采用 MIT License，公开维护 GitHub 与 GitLink 双仓库，README、示例网表、变更记录、测试和 CI 同步维护。开发阶段先以分压器、RC/RL 响应、二极管工作点等标准算例建立回归测试，再与 ngspice/LTspice 的节点电压和支路电流交叉验证；每个数值结果标注参考值、容差和近似边界。CI 使用 MoonBit 0.10.3 进行 `moon fmt --check`、`moon check --deny-warn`、`moon info` 和 `moon test --deny-warn`，保证项目可复现、可检查、可持续扩展。
